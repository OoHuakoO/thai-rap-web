NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_APP_NAME=Thai Rap
# Set to true in .env.local to intercept API requests with MSW mock handlers.
# Never set this in production — the code has a hard NODE_ENV=production guard.
NEXT_PUBLIC_ENABLE_MOCKS=false
