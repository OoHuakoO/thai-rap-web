.next
.next-verify
node_modules
dist
build

# Generated by `msw init` — regenerated verbatim, formatting it is churn.
public/mockServiceWorker.js

# shadcn-managed. Per .claude/rules/shadcn-component-rules.md these are never
# edited by hand; customisation belongs in components/shared/ wrappers.
components/ui

# Design reference exported from the original mockup — kept byte-for-byte so it
# stays comparable to what the designer handed over.
design

# Docs are excluded because Prettier reformats the code inside fenced blocks,
# and those blocks are illustrative, not runnable. It rewrote the "don't
# hardcode query keys" example in frontend-rules.md from four separate keys
# into a single member-access chain, `['users']['Users']['user-list']['user']`,
# which no longer shows what it was written to show. Same class of damage in
# every rules/skills doc that demonstrates a wrong pattern on purpose.
*.md
